# Рекурсивно обчислити факторіал заданого числа.

print("Введіть число, для якого потрібно обчислити факторіал:")
n = int(input())
def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n-1)

print(factorial(n))
